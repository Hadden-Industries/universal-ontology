# Issue #42, snapshot v1

Acceptance decision reference: https://github.com/Hadden-Industries/universal-ontology/issues/42#issuecomment-5626746364

Reported accepted-by: MaksymShostak; reported accepted-at: 2026-09-10T23:20:19Z.
These supplied values require independent confirmation; capture does not approve.

Body SHA-256: `79dbb1657df5f67b237a29bcba9963d262d67df35629402d4020d4ce33800e19`

## Accepted change proposed for owner decision

Complete the repository-owned baseline compatibility and proportionate execution
work in WP6/WP7 of `docs/plans/sdlc-implementation-plan-2026-09-10.md`, using the
supplied detailed WP6/WP7 packages. Include the supplied WP4/WP5 documentation
rules for truthful host diagnosis and installed security capability qualification.
Risk: R2, because this changes baseline linkage at a trusted policy boundary.

The current Issue-snapshot contract remains supported. Reuse the existing ONI
three-function plan contract as a small shared module, strengthened at the native
Git/GitHub object boundary. Do not create a Markdown parser, approval service,
second preflight runner, result cache or distribution platform.

## Intended behavior and acceptance criteria

- AC-601: Local begin/resume/fingerprinting and the trusted PR validator support
  actual Issue snapshots and previously accepted committed UTF-8 plans. Preserve
  existing Issue title/body/schema/risk/state/version/history checks. Plan paths
  are canonical repository-relative POSIX paths under docs/plans with exact .md
  suffix; reject traversal, aliases, controls, whitespace, colon/stream ambiguity,
  redirects and non-regular Git entries. Preserve admitted Unicode filenames.
- AC-602: Native local Git and remote commit/tree/blob readers preserve exact
  repository, revision, path, mode, object ID and bytes. A plan must be the same
  regular-file blob/mode at base and head and match local committed bytes. Reject
  candidate-only, modified, renamed, deleted, type/mode-replaced, malformed or
  incomplete object evidence. Read candidate data without executing candidate
  code or widening the trusted workflow's credentials. Handle fork repositories
  according to the authoritative PR identities.
- AC-603: The plan route requires exactly one non-placeholder Baseline acceptance
  reference and a nonempty JSON array of distinct exact source lines, each present
  once in the accepted plan. Neither metadata nor blob equality authenticates
  acceptance. Keep owner decision/content binding and product correctness separate;
  never report a plausible fabricated reference as accepted merely because linkage
  passes. R0/R1 stay light, and the existing baseline-only Issue route stays distinct.
- AC-604: Use explicit finite read limits: 1 MiB per baseline blob, 8 MiB per
  native GitHub JSON response, and 30 seconds per native metadata subprocess.
  Reject oversized, truncated, undecodable or unavailable input with truthful
  diagnostics; no silent fallback or baseline repair. Current retained baselines
  must be checked against these limits before adoption.
- AC-605: The existing task/handoff guidance exposes only relevant trusted-base,
  runtime, security-inventory and dependency prerequisites before expensive work.
  At remote handoff, distinguish actual policy/base/head/tested revisions and
  workflow event/run/attempt/jobs, including failures and required skipped work.
  An old event rerun does not assert deployment of newly merged trusted policy.
- AC-606: Cadence guidance separates risk from rerun frequency, keeps operational
  progress out of tracked verification inputs, reuses still-applicable decisions,
  and preserves actual final verification, required independence, fingerprints,
  native security ownership and original failures. Helper-only changes select
  the existing SDLC CI controls. No changed assurance threshold or profile is implied.
- AC-607: WP4/WP5 wording distinguishes requested shell, native diagnosis and actual
  host interception; installed workflow inventory, other omissions and Windows
  artifact access retain separate evidence and retirement decisions. Preserve the
  already approved programme-wide R2 alternative and the user's review deferral.

## Verification and adoption boundary

Use native Git fixtures, the existing .venv, current consumer validators and
focused red/green or preservation checks. Qualify malformed paths/bytes/objects,
fork identity, duplicate metadata and source-line ambiguity, both baseline formats,
current-evidence invalidation and helper-only CI selection. Run the required full
profile on the final frozen elevated-risk object. Independent reviews remain
deferred until the requested implementation changes are complete, as directed by
Max; this is not a waiver or a claim that deferred assurance has occurred.

Land trusted format support through this genuine policy change before relying on
it for an adopter's feature PR. Source implementation, trusted deployment, host
qualification and product outcome remain distinct. Actual MCP/SHACL integration,
ONI/WebVOWL changes, host/plugin updates, permission or branch-rule changes,
resource disposal and product publication require their actual separate scope.

