# SHACL policy SLICE-000: active set, candidate and integration inventory

Status: SLICE-000 evidence for the R2 task begun on 12 September 2026 against the
accepted committed plan
[`docs/plans/2026-09-09-shacl-policy-source-of-truth.md`](../plans/2026-09-09-shacl-policy-source-of-truth.md)
(plan blob `2581750b851a2d6fd78deb429ee2fb295d9617e9`, merged in
[PR #55](https://github.com/Hadden-Industries/universal-ontology/pull/55), owner
acceptance `pull/55#issuecomment-5643322518`). Starting head
`b3394e1b9cede8b347ae799eaf8bb1b87fd1bf14`. Observations are local and dated;
they are discovery inputs, not approval of any configuration change.

## Baseline and Issue reconciliation

- Plan, dossier, rule inventory, Issue body and independent review are regular
  files in `main` since PR #55. The plan's own text remains unchanged.
- Issue #31 body was replaced with the reconciled 12 September content (two
  self-status paragraphs updated to cite PR #55 and the committed-plan route);
  labels `sdlc:change`, `risk:R2`, `state:implementing`. A transient
  `state:changed` label was applied by the acceptance workflow because the body
  edit and the `state:accepted` label landed in one edit event; Max removed it.
  Lesson: edit the body first, then change the lifecycle label separately.
- The implementation PR uses the committed-plan route (`Change issue: none`,
  `Accepted baseline: docs/plans/2026-09-09-shacl-policy-source-of-truth.md`,
  `Baseline acceptance:` the comment reference above).

## Actual active publication set (observed 12 September 2026)

Live served `latest` aliases at `https://haddenindustries.com/ontology/` were
fetched and compared byte-for-byte with the tracked dated artifacts.

| Module                 | Served `latest` version IRI            | Local dated artifact                    | Live bytes == local |
| ---------------------- | -------------------------------------- | --------------------------------------- | ------------------- |
| universal/core         | `universal/core/20260714`              | `src/universal/core/20260714`           | yes                 |
| universal/extended     | `universal/extended/20260714`          | `src/universal/extended/20260714`       | yes                 |
| universal/reference-data | `universal/reference-data/20260714`  | `src/universal/reference-data/20260714` | yes                 |
| iso/31073/ed-1         | `iso/31073/ed-1/20260626`              | `src/iso/31073/ed-1/20260626`           | yes                 |
| iso-iec/11179/-3/ed-4  | `iso-iec/11179/-3/ed-4/20260714`       | `src/iso-iec/11179/-3/ed-4/20260714`    | yes                 |

This is the **latest active set** for initial qualification. Owned import pins
inside these artifacts: core → reference-data/20260714; reference-data →
iso-iec/11179/-3/ed-4/20260714; extended → core/20260714. External support
imports (`time/gregorian`, `skos/core`) are separately pinned context.

`ontologyAliases.js` selects the largest dated filename per directory; today that
coincides with the served set, but it is not the authority. The `*-full` merged
artifacts are generated by `scripts/create_full_versions.py` and excluded from
validation selection; they are derived outputs, not additional owned versions.

## Working files versus the active set (candidates)

| Working file                         | Declared version IRI            | Relation to active set                                                  |
| ------------------------------------ | ------------------------------- | ----------------------------------------------------------------------- |
| `core/universal-core.owl`            | `core/20260714`                 | Byte-identical to the active artifact.                                  |
| `iso-31073/iso-31073.owl`            | `iso/31073/ed-1/20260626`       | Byte-identical to the active artifact.                                  |
| `iso-iec11179-3/iso-iec11179-3.owl`  | `iso-iec/11179/-3/ed-4/20260714`| Byte-identical to the active artifact.                                  |
| `reference-data/reference-data.owl`  | `reference-data/20260714`       | **Draft under the active version IRI**: adds `vann:preferredNamespacePrefix`, and changes one entity's definition with `dcterms:modified` 2026-08-30. Different bytes reuse an existing version identity; a candidate must receive a new version. |
| `extended/universal-extended.owl`    | `extended/20260721`             | **Draft candidate**: no `src/universal/extended/20260721` artifact exists; 64/72 line delta from 20260714. Imports `core/20260714` (consistent with the active set). |

Consequence for the plan: the crossover qualification input is the five active
artifacts above. Two drafts exist; the reference-data draft's reuse of `20260714`
is the concrete instance of the dossier's "different bytes at an existing version
path fail" counterexample and must be resolved as data work with its own approval.

## Integration surfaces inspected

| Surface | Observed behavior | Implication |
| --- | --- | --- |
| `scripts/validate_ontologies.py` | Selects working/dated/dist paths from `--staged`, `--diff-base/--diff-head`, `--all-current` or explicit paths; runs `tests/universalontologytest.py` per file; treats any stdout or nonzero exit as failure. | Retained CLI and pre-install `--plan` selection; the new package replaces `execute_validation` and the output-means-failure contract at cutover. |
| `.githooks/pre-commit` (via `core.hooksPath`) | `validate_ontologies.py --staged` with `.venv` Python. | Hook boundary for draft diagnostics; no Java on the ordinary hook. |
| `.github/workflows/ontology-validation.yml` | `contents: read`; Python from `.python-version`; floating `pip install -r requirements.txt`; diff-based selection; `workflow_dispatch` runs `--all-current`. | Needs locked install, policy QA and Linux/Windows qualification jobs; exact patch to be approved. |
| `scripts/setUpDevelopmentEnvironment.js` | Creates `.venv`, floating `pip` upgrade, then sequential unpinned installs of `requirements.txt` and `requirements-sdlc.txt`. | Must consume the approved lock; exact setup change to be approved. |
| `scripts/build/ontologyAssets.js`, `ontologyAliases.js`, `sourceInventory.js` | Build `dist/` from `src/`; no editing-policy validation. | Publication gate must bind the exact `src` bytes consumed here to a qualification result. |
| `scripts/upload_to_s3.py` | Uploads existing `dist/` via an external `../../amazon-aws` helper with `--delete`; no rebuild, no receipt check. No deploy workflow exists; deployment is manual. | The actual publication control is this manual command plus the external helper; the external helper is outside this repository and remains a proof gap. |
| `.sdlc/verification.json` `full` profile | "Ontology source invariants" runs the legacy validator on the five working files. | To be replaced by full latest-active/candidate validation and policy checks. |

## Toolchain observed on the Windows host

- Python 3.14.7 in `.venv` with pip 26.2.1. Native `pip lock` (PEP 751) exists
  but is experimental and hashed only the running platform's wheels when tried,
  which is why a resolver was selected (decision 1 below). `rdflib 7.6.0` was
  installed; `pyshacl` was not until the locked setup ran.
- Java: an Oracle JDK was briefly installed and rejected; Temurin JDK 25.0.4.1+1
  replaced it (decision 2 below). `PATH`/`JAVA_HOME` still resolve the older
  Temurin 25.0.4+7 JRE, so qualification commands must name the JDK path
  explicitly. Jena 6.2.0 not installed.
- Linux qualification is unavailable locally; it requires the approved workflow.

## Decisions taken on 12 September and items carried into SLICE-001

1. Lock tool: Max chose pip-tools 7.6.1 (`pip-compile --generate-hashes`) over
   native `pip lock` (experimental; hashes only the running platform's wheels) and
   uv (largest new surface). `requirements.lock.txt` carries hashes for every
   published artifact of each pin, so one file serves Windows and Linux; markers
   are flattened, so `colorama` is installed on Linux too. pip-tools 7.6.1 emits a
   spurious `--no-index` in the header unless `CUSTOM_COMPILE_COMMAND` names the
   real command; the committed header is the verified reproduction command.
2. JDK: Temurin only, never Oracle. Temurin JDK 25.0.4.1+1 is installed at
   `C:\Program Files\Eclipse Adoptium\jdk-25.0.4.101-hotspot`; `.java-version`
   and the workflow pin are still to be proposed.
3. The two drafts (extended `20260721`, reference-data reuse of `20260714`) are
   acknowledged by Max as candidate qualification cases; no data action yet.
4. Effort re-estimate after SLICE-001's engine/command proof.
