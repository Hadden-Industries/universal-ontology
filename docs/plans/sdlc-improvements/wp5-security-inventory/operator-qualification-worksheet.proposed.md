# WP5 — operator qualification worksheet

> Historical planning document. File references were updated during organization; proposal and recorded outcome statements retain their original scope. See the [initiative index](../README.md) for completion context and original delivery identities.

**Status:** blank proposed worksheet. No field below is proof of an installed capability, approval or completed scan. Use one section in the existing task/PR/approved operational record; this file is not a new authoritative database.

## 1. Task and authority

| Field | Actual value or explicit gap |
|---|---|
| Existing task / parent WP5 / A10 reference | [not recorded] |
| Accepted scope and risk route | [not recorded] |
| Operator and accountable policy owner | [not recorded] |
| Approved inspection/helper operation | [not recorded] |
| Approved update/import action, if any | [not recorded] |
| Separate native scan authority, target and budget, if any | [not recorded] |
| Exact host artifact operation authority, if any | [not recorded] |
| Private retained evidence location and permitted readers | [not recorded] |
| Independent reviewer required by route | [not recorded] |

Do not treat an update, source read or user request for this plan as live scan, configuration or Git write authority. Reuse already recorded applicable approvals rather than creating redundant decisions per case.

## 2. Effective installation before and after any update

| Item | Before | After / unchanged / unavailable |
|---|---|---|
| Actual desktop/terminal surface | [unknown] | [unknown] |
| Application identity | [unknown] | [unknown] |
| Actual running/bundled engine identity | [unknown] | [unknown] |
| Separately installed CLI identity, if relevant | [unknown] | [unknown] |
| Curated/local/imported/admin-synced channel | [unknown] | [unknown] |
| Selected plugin ID and exposed manifest version | [unknown] | [unknown] |
| Loaded native skill/context locator | [unknown] | [unknown] |
| Exposed plugin root and canonical helper identity | [unknown] | [unknown] |
| Loaded selection dependencies, where exposed | [unknown] | [unknown] |
| Native helper Python identity | [unknown] | [unknown] |
| Repository `.venv` identity for repository checks | [unknown] | [unknown] |
| Relevant native settings and accepted `SECURITY.md` | [unknown] | [unknown] |
| Current permissions/trust and required writer | [unknown] | [unknown] |
| Actual supported refresh/reload action and response | [not applicable yet] | [not performed] |

Record exact accessible paths/digests in restricted evidence as appropriate. Do not dump all environment variables or unrelated private sessions. A same-name helper in a downloaded repository is not the installed root. No exposed version or source revision means “not exposed”, not guessed from the latest public commit.

## 3. Baseline preservation

Prior installed observation: [genuine previous run or not available].

Original command/mode, target, output, native exit and retained identity: [not recorded].

Do not reinstall an obsolete version solely to manufacture RED evidence. Do not overwrite a prior list with the candidate run. Update unavailable: [actual reason], next actor: [name], reassess at: [concrete event]. No full scan is used as a rollout poll.

## 4. Frozen source target

| Identity | Value |
|---|---|
| Physical repository and native root | [unknown] |
| Intended diff semantics and mode | [unknown] |
| Full base commit and selection rationale | [unknown] |
| Full head commit | [unknown] |
| Index identity when material | [unknown] |
| Required working/untracked content identity | [unknown] |
| Relevant explicitly scoped ignored material | [unknown or none with reason] |
| Accepted input/consumer checkpoint | [unknown] |
| Output directory outside selected diff | [unknown] |
| Before/after movement observation and limits | [unknown] |

Local-patch membership is not proof of index-only review. Preserve divergent staged and working bytes as different inputs. Committed mode uses the chosen base/head rather than a moving branch or assumed merge base.

## 5. Matrix results

Use the catalogue T501–T532 and fixture recipe. For each actually run scenario retain:

| Field | Value |
|---|---|
| Scenario ID and exact fixture/candidate | [not run] |
| Installed helper/runtime identity | [not recorded] |
| Native argv, working directory and output target | [not recorded] |
| Actual start/end and exit status | [not recorded] |
| Raw stdout/stderr and inventory identity | [not recorded] |
| Independent literal expected set E | [not recorded] |
| Actual set I and duplicate/malformed rows | [not recorded] |
| Missing / unexpected paths | [not recorded] |
| Result: passed / failed / unavailable / not-run / justified N/A | [not run] |
| What this does not prove | [installed/helper/desktop/analysis boundaries as relevant] |

Core qualification uses the actual supported Windows path. Source-only/Linux checks stay separately labelled. Conditional unsupported names, filesystems or legacy interfaces constrain scope; they do not justify permissions expansion.

## 6. Real-target path reconciliation

G = complete Git obligations; R = accepted security-review scope; I = direct installed inventory; D = actual native prepared/listed inventory where used.

| Path or exact path group | Git status and old/new sides | Required content identity | Native selection / omission | Security-review obligation | Accepted method/decision | Evidence / remaining gap |
|---|---|---|---|---|---|---|
| [not populated] | [unknown] | [unknown] | [unknown] | [unknown] | [unknown] | [unknown] |

Do not insert “all tests excluded, therefore safe”. Groups need identical reasoning and identifiable member paths. A renamed destination does not erase the source/baseline obligation. Equal counts do not establish equality; unexpected files are reported separately from missing files.

## 7. Native route and scan — only when separately authorised

| Item | Actual result |
|---|---|
| Existing scan continuation or authorised single start | [not started] |
| Native scan ID, authoritative directory and target | [not recorded] |
| Native configuration/capability preflight | [not run] |
| Effective helper/runtime binding | [not recorded] |
| Prepare review-items result | [not run] |
| All inventory pages/cursors and actual set | [not run] |
| Required paths/content sides fully accounted for | [not established] |
| Required artifact writer/identity readback | [not run] |
| Existing Windows procedure used with exact authority | [not performed] |
| Native source-review/candidate/coverage outcome | [not run] |
| Native finalisation and completed-bundle retrieval | [not run] |
| Actual deferred coverage and unresolved failures | [not assessed] |
| Native usage/duration, if exposed | [unavailable unless observed] |

No new scan is started simply to complete this table. A direct helper pass does not prefill any of these fields. Preserve native continuation on recovery; do not replace or forge scan state.

## 8. Separate workaround decisions

| Item | Cause and original authority | Current evidence | Decision and exact scope | Next actor / reassessment |
|---|---|---|---|---|
| W5-WORKFLOW | [not recorded] | [not run] | [pending] | [actor/event] |
| W5-OTHER-SCOPE | [not recorded] | [not assessed] | [pending / retained] | [actor/event] |
| W5-WINDOWS-IO | [not recorded] | [not run] | [retain unless separately qualified] | [actor/event] |
| General path accounting | Existing SEC-01 obligation | [actual record] | Retained; not a workaround | Every applicable review |

No R0/R1 standing exception is extended to R2/R3 by analogy. Preserve ONI's exact historical 70/164 snapshot and direct-review decision; compare a new target using its own denominator. The later PR file count is not the historical missing-path count.

## 9. Optional Windows retirement evidence

Only populate when the owner is actually retiring the artifact procedure:

Fresh scan/directory: [not created]. Required native writer and identity: [unknown]. Direct authorised artifact creation/readback without the workaround: [not run]. Native completion: [not run]. Completed bundle reopened: [not run]. Restart readback required by accepted criterion: [not run]. Broader ACL/permission/source changes: [none authorised by this worksheet].

If upstream replaces the reviewer write requirement with a native-owned writer, retain the exact new contract and the owner's accepted revised criterion; do not misdescribe which identity wrote the file. Otherwise keep the existing procedure and its bounded authority unchanged.

## 10. Handoff

Status: [installed capability pending / installed selector qualified but native route pending / workflow selection qualified with other obligations retained / WP5 accepted for recorded environment].

Owner decision and exact scope: [not recorded]. Qualified modes: [none demonstrated here]. Required unqualified modes: [unknown]. Original native/alternative evidence: [locators]. Current native scan status: [not started or actual identity]. Policy text and final source/CI verification: [not applied/not run]. Next actor and event: [required].

No product release, cross-repository adoption, worktree removal, permission expansion, findings publication or retroactive native completion is implied.
