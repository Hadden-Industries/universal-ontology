# WP5 implementation package

> Historical planning document. File references were updated during organization; proposal and recorded outcome statements retain their original scope. See the [initiative index](../README.md) for completion context and original delivery identities.

Start with `implementation-plan.md`.

This is a researched implementation/qualification proposal, not an installation, security verdict, accepted policy or native scan.

The package contains the detailed plan, exact proposed amendments, a blank operator worksheet, a bounded fixture recipe and a 32-scenario catalogue. The only execution reported is the separate 12-check native-Git/exact-set-oracle exercise in fresh synthetic local data. It did not invoke Codex Security, its helpers, a Windows host or workflow jobs. T501–T532 and O51–O52 remain not-run.

`wp5_git_oracle_check.py --output <new-path>.json` reproduces that limited exercise using the local Python and Git selected by the operator. Review its fixture operations before use; it creates a new temporary repository only, with no network or remotes. It does not qualify an installed plugin.

`artifact-checks.json` verifies package consistency only. `original-delivery-manifest.json` identifies supplied files and parent inputs; it is not a signature or evidence of human acceptance.

Do not copy the native plugin implementation into the repository. Do not apply proposed text or start live scans without the relevant existing authority.

## Organized-copy boundary

The original delivery manifest and check results describe the historical package, before documentation references were edited. They do not verify this reorganized copy. The retained Python files are historical planning recipes; their embedded original filenames are not maintained execution instructions. Current navigation is checked separately.
