# WP6 implementation-plan package

> Historical planning document. File references were updated during organization; proposal and recorded outcome statements retain their original scope. See the [initiative index](../README.md) for completion context and original delivery identities.

Start with `implementation-plan.md`.
This expands the supplied parent's WP6/A11/A12 and is a proposal, not an implemented SDLC change.

The policy amendments and preflight/remote worksheet are implementation aids, not additional acceptance authorities. The verification catalogue has 40 scenarios and two operational exercises, all not-run. Its twelve acceptance criteria remain not-assessed.

The 18-check native Git/codec report records supporting research in an owned no-remote Linux repository, not either user's validator or Windows/native acceptance. The 39-check artifact report validates document consistency and the retained research record. The earlier artifact-checker syntax failure is separately retained under `planning-checks`; it was corrected and never counted as passed.

`remote-observations.json` is an explicitly derived readback of the inspected GitHub state/logs, not a native export, fresh run or owner decision. Refresh the real state at implementation.

No dependency binaries, token, plugin, new baseline schema, global installer, live scan or automatic cleanup is included. The operator recipe is syntax-checked, not executed against user repositories.

`original-delivery-manifest.json` identifies all other payload files; it is an integrity aid, not proof of correctness or approval. No self-hash is claimed. Source links in the main plan preserve research provenance. Relative companion links work when the ZIP is extracted as one directory.

## Organized-copy boundary

The original delivery manifest and check results describe the historical package, before documentation references were edited. They do not verify this reorganized copy. The retained Python files are historical planning recipes; their embedded original filenames are not maintained execution instructions. Current navigation is checked separately.
