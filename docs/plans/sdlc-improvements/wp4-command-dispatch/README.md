# WP4 implementation package

> Historical planning document. File references were updated during organization; proposal and recorded outcome statements retain their original scope. See the [initiative index](../README.md) for completion context and original delivery identities.

Prepared 10 September 2026 for the WP4 work package in `../implementation-plan.md`.

Start with `implementation-plan.md`.
The operator worksheet and policy amendments are proposals, not accepted policy or executed evidence.
`test-catalogue.proposed.json` describes future tests; every execution status is `not-run`.
Files under `reproductions/` are text data transcribed from the published Issue #30 and its comment, not executable scripts and not captured hook-request bytes.

No DCG executable, host-hook installer, classifier, bypass, deletion program, credential or runtime schema is included. The artifact-check script checks this package's consistency only. It never executes the reproduction text, invokes DCG, calls an agent, or accesses a user's repository.

The plan's source register records immutable repository references where available. Mutable documentation and release observations must be refreshed at implementation. Retain original failures and approvals; neither this package nor a test result supplies deployment authority.

## Organized-copy boundary

The original delivery manifest and check results describe the historical package, before documentation references were edited. They do not verify this reorganized copy. The retained Python files are historical planning recipes; their embedded original filenames are not maintained execution instructions. Current navigation is checked separately.
