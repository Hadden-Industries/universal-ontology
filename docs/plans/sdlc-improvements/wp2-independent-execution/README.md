# WP2 implementation-plan package

> Historical planning document. File references were updated during organization; proposal and recorded outcome statements retain their original scope. See the [initiative index](../README.md) for completion context and original delivery identities.

Prepared 10 September 2026 for the Universal Ontology SDLC.

## Start here

Read `implementation-plan.md`. It expands the supplied parent WP2 and Issue #33 into nine implementation slices, 32 regression scenarios, two procedure/product exercises and twelve acceptance criteria.

`policy-amendments.proposed.md` supplies exact proposed wording for three existing policy/skill owners. Neither document is applied code, accepted policy or an authorization to change repositories.

## Supporting research

`publication-mechanism-probe.py` is a bounded standalone research experiment on synthetic temporary data. Its supplied JSON records an actual Linux/Python 3.13.5/Git 2.47.3 run with fourteen matching mechanical observations. This does not execute the repository SDLC and does not qualify Windows, NTFS, native agent/DCG dispatch, or the full linked-worktree lifecycle. Review the source and existing permissions before an authorized replay. No cleanup tool is supplied.

The package contains no user worktree content, original runtime evidence, credentials, dependencies or modified repository files. The original input documents remain separate and their SHA-256 identities are bound in the plan.

`original-delivery-manifest.json` records file hashes and mechanical document checks. Those checks are not semantic acceptance, independent review or implementation verification.

## Organized-copy boundary

The original delivery manifest and check results describe the historical package, before documentation references were edited. They do not verify this reorganized copy. The retained Python files are historical planning recipes; their embedded original filenames are not maintained execution instructions. Current navigation is checked separately.
